mixin teamCard(src, srcRetina, name, profession, desc)
  article.team-card
    h3.team-card__name= name
    .team-card__photo
      img.team-card__img(src=src srcset= srcRetina alt="фотография члена команды")
    span.team-card__profession= profession
    p.team-card__desc= desc