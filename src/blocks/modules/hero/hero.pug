include ../../components/btn/btn.pug

section.hero
  include ../header/header
  .container.hero__container
    h1.title.hero__title беспроводные наушники beats solo 3
    h3.hero__text Встречайте новую версию наушников из линейки Powerbeats.
    +btn('#', 'заказать')
    .hero__list
      each item in heroList
        .hero__item
          <svg class="hero__item-icon">
            <use xlink:href="../img/svg/sprite.svg#{item.id}"></use>
          </svg>
          .hero__item-text= item.text
    .hero__picture
      img.hero__img(src='../../../img/hero/MX452.png' alt='наушники beats')

